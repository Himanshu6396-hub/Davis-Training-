def calculatesi(p,r,t)
    return ((p*r*t)/100)
#________________
#Main program 
principal=float(input("Enter the principal amount"))
rate=float(input("Enter the rate(in%)"))
time=int(input("Enter the time"))
print("Simple Interest is:",calculatesi(principal, rate, time))
